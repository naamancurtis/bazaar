mutation createCustomer(
  $email: String!
  $firstName: String!
  $lastName: String!
) {
  createCustomer(email: $email, firstName: $firstName, lastName: $lastName) {
    id
    firstName
    lastName
    email
    createdAt
  }
}
